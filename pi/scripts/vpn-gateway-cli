#!/bin/sh
# Fixed root-owned wrapper; the Python CLI validates all accepted subcommands.
# The current symlink is switched atomically by the signed updater.
exec /opt/vpn-gateway/releases/current/venv/bin/vpn-gateway-cli "$@"
